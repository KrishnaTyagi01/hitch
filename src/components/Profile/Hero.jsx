import heroImg from '../../icons/profile/hero.png';

const Hero = ()=>{
	return(
		<div className="hero">
			<img src={heroImg} alt="hero img" className="hero__img"/>
		</div>
	)
}

export default Hero;